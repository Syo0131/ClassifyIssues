# Bloquear ejecución en npm/yarn si se usa un packageManager específico (requiere corepack habilitado pero es buena práctica)
engine-strict=true

# Evitar instalación de dependencias fantasma (activado por defecto en pnpm, explicitado aquí por seguridad)
shamefully-hoist=false
